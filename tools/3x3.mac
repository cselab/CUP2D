load("cgrind");
A: matrix([xx, 0, xz], [0, yy, yz], [xz, yz, zz]);
b: matrix([bx], [by], [bz]);

D: determinant(A);
M: lu_factor(A);
x: lu_backsub(M, b);

fullratsimp(A . x - b);

x: map(fullratsimp, x * D / 'D);

cgrind(x[1]);

